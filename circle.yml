machine:
  node:
  php:
  services:
    - docker

dependencies:
  override:
    - cd moj-be && make build
    - cd moj-fe && make build

test:
    pre:
        - cd moj-fe && make test
        - cd moj-be && make test

deployment:
    production:
        branch: master
        commands:
            - echo "Deploy to production now"
