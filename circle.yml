machine:
  node:
  php:
  services:
    - docker

dependencies:
  override:
    - cd moj-be && make build && make push
    - cd moj-fe && make build && make push

test:
    pre:
        - cd moj-fe && make test
        - cd moj-be && make test

deployment:
    production:
        branch: master
        commands:
            - echo "Deploy to production now"
